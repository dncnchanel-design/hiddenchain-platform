# 前端产品化整改审计

## 技术基线

- 框架：React 18、TypeScript 5.7、Vite 6、React Router 7。
- 包管理器：pnpm，锁文件为 `frontend/pnpm-lock.yaml`。
- UI：自定义 CSS 与共享 React 组件；图表使用 Recharts；图标沿用现有 Lucide 依赖。
- 状态：组件本地状态与 Auth Context；接口通过原生 `fetch` 封装。
- 交付命令：`pnpm lint`、`pnpm typecheck`、`pnpm test` 与 `pnpm build` 均已通过。
- 已补充 ESLint flat config、Vitest 访问策略与格式化单测；真实浏览器回归覆盖权限、键盘焦点、控制台和三档桌面视口。

## 共性问题与整改目标

1. 当前单一侧栏混排业务与管理菜单；改为两个独立工作空间和统一权限配置。
2. 越权路由跳回 `/overview`，未知路由也重定向；改为正式 403、404 与会话失效页。
3. 令牌保存在 `localStorage`；改为会话级存储并统一 401 处理。
4. `DataTable` 仅负责静态渲染；升级为分页、排序、列控制、固定表头、首列左冻结、操作列右冻结和表格内部滚动。
5. `useRemote` 没有把 AbortSignal 传入 loader，卸载与依赖变化只忽略结果但不取消请求；改为可取消、可超时、可区分首次加载与刷新失败。
6. 页面级早退使慢请求期间只剩孤立加载图标；改为结构化骨架与保留页面上下文的错误状态。
7. 日期、缺失值、金额、电量、百分比和长编号格式不统一；集中到 formatter 与展示组件。
8. Modal 没有焦点捕获和关闭后焦点恢复；危险操作缺少统一确认框。
9. 多处页面直接展示原始英文代码或整段 JSON；业务视图中文化，技术值进入受控详情区并脱敏。
10. 现有 18 路由截图已建立基线，其中 `/data/retail`、`/rules` 只显示加载态，`/logs` 形成超长页面。

## 已知根因

### `/data/retail` 与 `/rules`

- 两页使用相同的 `useRemote` + `api` 链路。
- `api` 本身有 15 秒超时，但 `useRemote` 声明了 `AbortSignal` 却调用 `loader()`，导致路由切换和依赖变化无法真正取消旧请求。
- 两页在首次请求结束前整页早退到通用 spinner；截图脚本在请求仍未完成时得到看似无限的加载页面，用户也没有取消、超时进度或局部上下文。
- 整改：请求信号贯通、超时文案与 Trace ID、表格骨架、刷新与首次加载分离，并在真实后端上验证请求最终进入数据、空或错误状态。

### `/logs`

- 后端 `/audit/logs` 固定最多返回 500 条，当前前端筛选后把全部记录交给 `DataTable`，没有分页或虚拟化。
- 整改：不改接口契约，前端默认 20 条分页，支持 20、50、100；筛选时回到第一页。当前后端没有可授权、可记录的导出端点，因此不在浏览器侧伪造导出能力。

### `/metrics`

- 当前同一柱状图混合次数、百分比和毫秒，纵轴无法解释。
- 整改：按“耗时”“比率”“计数”拆分指标和图表，单位写入标题或轴标签。

## 逐路由审计

| 路由 | 基线问题 | 整改结果目标 | 状态 |
| --- | --- | --- | --- |
| `/login` | 大幅深蓝宣传分栏；缺少 Caps Lock、版本、单位、安全提示和环境信息 | 紧凑品牌区 + 中宽登录面板；通用失败提示；完整辅助信息 | 已完成 |
| `/workbench` | 管理员工作台展示系统服务健康，业务待办与身份卡重复 | 按角色显示待办、风险和最近业务活动；移除业务空间服务健康模块 | 已完成 |
| `/overview` | 普通业务角色也可进入，标题和职责混用 | 仅管理控制台显示“管理总览” | 已完成 |
| `/data-space` | 批次查询与两张全量表缺少分页、详情和列控制 | 数据产品与调用列表统一筛选、分页、协议详情 | 已完成 |
| `/data/generation` | 全页加载早退，表格功能不足 | 与用电侧统一布局、筛选、状态、分页和详情 | 已完成 |
| `/data/retail` | 基线截图停留加载态 | 修复请求生命周期并保证超时、空、错误和数据状态闭环 | 已完成 |
| `/rules` | 基线截图停留加载态；字段偏技术化 | 修复加载；仅展示接口真实提供的名称、范围、版本与状态；技术内容折叠 | 已完成 |
| `/compute` | 计算方案卡片化且任务表缺分页；技术细节权重过高 | 紧凑方案列表 + 任务列表，业务说明优先 | 已完成 |
| `/settlements` | 主从布局可用但缺筛选、分页和高风险确认 | 保留真实“调用验证”职责，补充任务筛选与运行确认 | 已完成 |
| `/results` | 长任务编号和机构名可能挤压金额；主体金额会被重复汇总；确认缺二次确认 | 统一金额和机构列，按任务汇总去重，标明应收/应付，确认动作显示对象与后果 | 已完成 |
| `/evidence` | 批量核验串行且无分页；详情直接展示完整 JSON | 分页、阶段筛选、详情技术区和明确核验反馈 | 已完成 |
| `/audit` | 仍带 AI 对话式提问和建议问题按钮 | 改为“审计复核”，事件时间线 + 复核队列 + 明确非确定性的证据辅助解释 | 已完成 |
| `/reports` | 生成区与列表可用但浏览器侧导出无法授权或留痕 | 任务/模板生成、分页列表与详情；未开放的服务端审计导出明确隐藏 | 已完成 |
| `/anomalies` | “注入”语义仍偏演示；处置无正式确认 | 改为风险登记/处置语言，指标与列表一致，危险操作二次确认 | 已完成 |
| `/logs` | 一次渲染最多 500 条，基线截图为超长页面；搜索序列化整行对象 | 管理控制台分页日志、字段定向搜索和详情脱敏 | 已完成 |
| `/system` | 名称为身份管理，缺组织与权限整体语义 | 统一为“组织与权限”，组织、用户、凭证视图一致 | 已完成 |
| `/agents` | 暴露模型、Token、自由提示词和调试台特征 | 仅管理控制台可见；改为“能力与服务”列表/编排，技术细节折叠 | 已完成 |
| `/metrics` | 名称为验收指标；单图混合不同单位 | 改为“运行监控”，按单位拆分并保留真实测量范围 | 已完成 |

## 共享组件整改范围

- 布局：`AppShell`、`BusinessLayout`、`AdminLayout`、`WorkspaceSwitcher`。
- 权限：路由策略、`PermissionGuard`、403、404、会话失效页。
- 数据：`DataTable`、`FilterBar`、`MetricStrip`、分页和列配置。
- 格式：`IdText`、`AmountText`、`DateTimeText` 和集中状态映射。
- 反馈：结构化 `LoadingState`、`EmptyState`、`ErrorState`、`ConfirmDialog`、`DetailDrawer`。
- 审计：`AuditTimeline`、`RiskLevelTag`、技术信息折叠区与 Trace ID。

## 终审语义与安全收口

- 数据承诺签名：前端按“当前组织 = 数据归属组织”逐行授权；后端 `/api/data/{upload_id}/sign` 同步强制归属校验并移除管理员签署权限。新增越权签名 403 回归测试。
- 结果金额：管理视角只汇总每个任务的 `SUMMARY` 结果，主体视角汇总自身 `ORG` 结果；主体结果明确显示“应收/应付”，不再把同一任务双方金额重复相加。
- 审计解释：`/agent/query` 明确呈现为生成式“证据辅助解释”，展示置信度、引用状态、生成方式和能力边界，并持续提示其不构成确定性核验、审批或合规结论。
- 可信复核：后端未返回布尔核验结论时显示“未核验”；存在结果摘要不再被推断为核验通过。
- 导出边界：结果、报告、日志三页撤下纯浏览器下载；页面说明当前环境未开放经服务端授权与审计的导出通道。
- 会话容错：仅 401 清除凭证；超时或 5xx 保留会话级令牌并进入可重试不可用页。断开并恢复后端的浏览器故障注入验证无需重新登录。
- 指标语义：工作台分开显示“已生成结果任务”“待处置风险事件”；管理总览改为“可信能力登记概况”，不再把登记统计表述为实时健康结论。
- 提交锁定：`ConfirmDialog` 忙碌时禁用取消和关闭按钮，并拦截 Escape 与背景关闭，避免请求在途时产生状态歧义。
- 事实约束：规则页移除后端未提供的固定类型/适用对象；数据主表移除内部数据引用；所有行操作列统一右侧冻结且不可从列设置隐藏。

## 验收记录

- 前端：ESLint 零告警；TypeScript project build 通过；Vitest 2 个测试文件、8 项用例全部通过；Vite 生产构建通过。
- 后端：pytest 46 项用例 100% 通过；保留 1 条来自 Starlette/httpx 兼容性的既有弃用警告。
- 浏览器：登录页与 17 个鉴权页面完成真实 API 回归；1366×768 全页面检查无可见错误、加载残留、整页横向溢出、重复 ID 或无名称可见控件。终审变更页另完成逐页语义断言、签名越权 403、会话故障恢复、生成式解释边界、未核验状态与忙碌弹窗锁定验证。
- 权限：售电企业不显示管理工作空间及管理菜单，直达 `/agents` 返回正式 403；未知路由返回正式 404。
- 可访问性：详情抽屉打开后焦点进入关闭按钮，Escape 关闭后焦点还给触发按钮；控制台无应用级 error/warn。
- 视觉：1366×768 全量页面、1440×900 与 1920×1080 代表性高密度页面均完成确认；34 张精确尺寸截图位于 `.impeccable/review/`。
- 反模式检测：修复 2 处会触发布局重排的宽度/外边距动画；未保留检测器临时文件。
