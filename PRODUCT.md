# 产品定义

<!-- impeccable:product-schema 1 -->

## 产品定位

“隐链明算”是面向电力、煤炭、热能、天然气和石油领域的可信数据空间。平台只保存企业公布的中文目录元数据、授权状态、计算任务、受控结果和审计存证；企业原始数据始终留在企业连接器中。

核心承诺是“数据可用不可见、可追溯可审计”。需要计算时，平台向持有数据的企业连接器发送经过 Ed25519 签名的固定函数任务，连接器在企业侧读取原始数据，只返回满足授权范围和隐私阈值的受控结果。

## 参与主体

- 发电企业、售电企业、煤炭企业、热能企业、天然气企业和石油企业：公布目录，决定是否授权、授权哪些字段、允许什么固定函数以及授权期限。
- 交易中心：统一显示为“交易中心”，后台按电力、煤炭、热能、天然气和石油分别隔离组织与数据范围。
- 监管方：可发起跨能源查询，但仍必须逐项向数据提供企业申请授权。
- 平台运维：只维护服务状态和脱敏技术日志，不查看或处理企业目录、授权、任务、结果和业务审计。
- 个人账号：必须属于一个企业或机构；企业最高权限账号向个人授予目录、授权审批、计算任务、结果查看等细粒度权限。

## 主导航

产品固定为八个一级模块：运行总览、智能数据查询、数据目录、数据连接、数据授权、审计追溯、隐私计算、参与主体。详情页归入上述模块，不额外增加一级入口。

## 数据与计算边界

- 原始数据不上传平台，不进入目录数据库、审计日志、任务记录或结果文件。
- 电力、煤炭、热能、天然气和石油使用五个隔离连接器与独立数据库。
- 只允许求和、平均值、最大值、最小值、计数、中位数、增长率、同比、环比、分组汇总、阈值判断、趋势、PSI 和 MPC 聚合等固定函数。
- 平台请求和企业结果使用 Ed25519 数字签名；时间戳、随机数和重复查询预算用于防重放与差分攻击控制。
- 当前真实能力为企业侧本地受控计算与追加写入审计哈希链；TEE、外部 MPC 运行时和外部区块链锚定在未配置时明确显示“未配置”或“已阻断”。

## 授权与交付

- 任何非公开数据查询都先创建授权申请，由数据提供企业批准后执行。
- 非监管主体不得跨能源范围申请；监管方可跨能源申请，但没有免审批通道。
- 授权撤销或过期后不能新建计算任务。
- 在线页面只展示受控结果。离线结果下载只有在所需各方审批完成后才允许；交付结果必须携带任务编号、授权范围、生成时间和数字签名，并写入审计记录。
- 数据资源缺少中文名称时显示“未命名数据资源，请由提供企业补充中文名称”，绝不回退显示英文资源 ID。

## 语言规范

页面以简体中文为主，不在每句中文后追加英文。DID、CSV、API、PSI、MPC、TEE 等行业缩写保留；内部状态码、资源 ID、适配器代码和英文枚举不得直接作为普通界面文案。

## 环境

- `demo`：公开演示环境，使用演示组织、中文目录元数据和隔离连接器，可在线验证授权与受控计算链路。
- `development` / `test`：允许旧兼容入口和确定性测试夹具，不作为公开产品能力。
- `production`：企业内网部署，必须使用独立密钥、明确 CORS、外部策略服务和干净数据库；旧原始文件入口默认拒绝。

## 验收原则

1. 数据不出域：平台数据库和接口中不存在企业原始记录。
2. 授权先行：没有企业批准的有效授权，连接器计算不能启动。
3. 结果可信：请求签名、连接器签名、任务编号、授权范围和审计记录能够互相核对。
4. 权限最小化：账号能力来自企业授权，不由固定“数据管理员”等岗位名称决定。
5. 能力如实：本地哈希链不称为区块链；未配置的外部能力不展示为已部署。
6. 中文可读：用户界面不暴露英文资源 ID，不使用难懂的自造缩写或中英重复句。
