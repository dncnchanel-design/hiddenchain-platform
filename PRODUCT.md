# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 电力交易中心：发起结算任务、选择规则、检查数据就绪状态并启动计算。
- 发电企业与售电企业：维护本组织数据引用、查看本方结果并独立确认。
- 监管与审计人员：复核任务、证据台账、异常和报告，不代替业务主体确认结果。
- 系统管理员：维护组织、身份、能力服务、指标和日志，不参与业务确认。

主要使用环境是 Windows 办公终端与封闭内网，核心工作是高频检索、任务处理、结果复核和审计追踪。

## Product Purpose

“隐链明算”是可白标的电力交易可信执行平台。系统以结算任务为业务主线，把参与方、数据引用、规则、授权、计算回执、结果确认、证据台账、异常和报告关联到同一任务。

本版本提供可运行的本地受控确定性结算，但不把本地进程包装成真实 MPC、TEE、区块链或跨域不出域证明。用户应能从业务页看到下一步，从技术页看到真实执行边界。

## Release Scope

| 能力 | 本版本状态 | 对外表述 |
| --- | --- | --- |
| 结算任务与五步创建 | 可用 | 独立页面、可保存未就绪任务 |
| 数据与规则预检 | 可用 | 基于数据库中的引用、签名与规则状态 |
| 结算计算 | 可用 | `LOCAL_CONTROLLED_SETTLEMENT_V1`，单进程确定性计算 |
| 结果双边确认 | 可用 | 发电、售电分别确认；系统不自动代签 |
| 证据记录与摘要核验 | 可用 | `LOCAL_EVIDENCE_LEDGER_V1`，本地数据库哈希台账 |
| API 最小披露 | 可核验 | 结算 API 不返回 Vault 原始记录 |
| 真实 MPC / TEE / 远程证明 | 未配置 | 只作为外部候选方案展示，不可执行 |
| 区块链存证 | 未配置 | 不显示“已上链”；现有表名仅为兼容历史结构 |
| 跨主体不出域证明 | 未提供 | 必须接入外部连接器/运行时并取得证明后才能声明 |

## Operating Model

- 任务状态统一为：`DRAFT → READY → RUNNING → PENDING_CONFIRMATION → PARTIALLY_CONFIRMED → AUDITED`，异常时进入 `EXCEPTION`。
- 可信执行链统一为八步：任务创建、主体确认、数据就绪、规则授权、受控计算、结果生成、双边确认、审计完成。
- 发起任务不要求所有前置条件立即满足；系统保存任务并给出阻塞项。启动计算才执行严格预检。
- 工作台按角色给出“下一步、责任主体、阻塞原因”，而不是展示无来源的健康分或宣传指标。

## Environments

- `development`：允许本地开发夹具和本地策略回退。
- `test`：允许确定性测试账户、测试数据、失败注入和验收导入。
- `production`：禁止夹具灌入、测试延迟、测试入口和本地策略回退；启动前验证配置与数据库，构建前运行静态门禁。

详细边界见 `docs/ENVIRONMENT_MATRIX.md` 与 `docs/DEMO_REMOVAL_AUDIT.md`。

## Brand Commitments

- 默认产品名是“隐链明算”，默认定位是“电力交易可信执行平台”。
- 产品名、简称、副标题、Logo、favicon、客户、运营方、建设方、版权、支持信息和登录公告均来自运行时白标配置。
- 品牌主题与上述配置统一归入 `ProductBrandConfig`；部署通过主题技术标识和一个品牌基准色生成完整色阶与语义 Token，不修改业务组件。
- 默认 `power-grid-green` 是可替换的电力行业青绿色视觉起点，不代表任何客户授权，也不在普通用户界面提供换肤入口。
- 未配置客户 Logo 时使用本地文字标识；不依赖外部字体、CDN 或统计脚本。
- 文案正式、直接、可核验；“已通过、已确认、已记录”只能来自对应事实。

## Product Principles

1. 任务优先：所有业务对象都能回到结算任务。
2. 事实优先：界面不把推荐方案、摘要存在或本地记录推断为安全认证。
3. 权限闭环：菜单、路由、操作和数据范围都由角色与后端共同控制。
4. 状态可行动：每个非终态都有责任人、下一步和阻塞原因。
5. 审计可定位：任务、时间、主体、规则、数据引用、回执、签名和证据保持关联。
6. 生产默认拒绝：缺少密钥、PDP、明确 CORS 或干净数据库时拒绝启动。

## Accessibility & Compatibility

- 支持键盘操作、明确焦点、语义表单、状态双重编码和弹层焦点管理。
- 覆盖 Chrome/Edge 当前及近两个主版本、1366×768、1440×900、1920×1080 与 Windows 125% 缩放。
- 长机构名、编号、哈希和金额在表格自身滚动，不依赖全页横向滚动。
