# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 电力交易中心工作人员：组织数据授权、调用验证与结算相关任务。
- 发电企业与售电企业人员：登记本组织数据引用、参与受控计算并确认结果。
- 结算与复核人员：检查任务、规则、计算回执与多方确认状态。
- 监管与审计人员：核验证据链路、复核结果、生成报告并处置风险事件。
- 系统管理员与运维人员：维护组织、身份、能力服务、运行指标和系统日志。

用户主要在 Windows 办公电脑和封闭内网中，以高频列表检索、任务处理、结果复核和审计追踪为核心工作。

## Product Purpose

“隐链明算”是电力交易可信执行平台。系统支撑电力与能源交易中的数据授权、受控调用、隐私计算、结果确认、审计追踪和风险处置，使业务数据在不改变既有后端契约和计算事实的前提下做到可用不可见、过程可审计、结果可追踪。

成功意味着用户能够按角色完成真实业务任务，任何关键操作、状态变化和异常都可定位到对象、时间、证据与处理结果。

## Positioning

平台以“授权规则 + 受控计算 + 最小结果披露 + 可核验证据链路”串联跨主体交易执行。它不把前端隐藏当作安全控制，也不以智能解释替代业务规则或人工裁决。

## Operating Context

- 逻辑上分为业务工作台与管理控制台，共用现有后端。
- 业务工作围绕数据目录、数据引用、授权规则、调用验证、隐私计算、结果确认、审计凭证、审计复核、报告和风险处置展开。
- 管理工作围绕组织与权限、能力与服务、运行监控和系统日志展开。
- 目标环境为 Chrome 与 Edge 当前及近两个主版本，覆盖 1366×768、1440×900、1920×1080 和 Windows 125% 缩放。
- 部署环境可能完全离线，不依赖 Google Fonts、外部 CDN、在线图标或第三方统计服务。

## Capabilities and Constraints

- 保持现有 FastAPI 接口路径、方法、请求与响应结构不变。
- 保持现有认证机制、结算计算、审计证据生成、数据库结构与真实业务数据不变。
- 展示层可使用 selector、adapter、view model 和 formatter 整理现有响应。
- 工作空间、菜单、路由和按钮从会话角色与后端返回菜单共同推导，不能只靠 CSS 隐藏。
- 无权直达路由进入正式 403 页面；会话失效进入明确流程，不无限重试。
- 不伪造安全认证、合规结论、行情、审批节点、数字证书或后端不存在的业务状态。
- 缺少现场后端证据时明确显示未提供或不可用，不用演示数据冒充生产事实。

## Brand Commitments

- 产品名称固定为“隐链明算”。
- 产品定位固定为“电力交易可信执行平台”。
- 保留现有盾形品牌标识、现有单位名称和必要组织标识。
- 语言正式、直接、可核验，避免宣传口号、研发术语和互联网营销表达。

## Evidence on Hand

- 现有 React 前端与 FastAPI 后端源码，位于本仓库的 `frontend/` 与 `backend/`。
- 后端返回的角色、菜单、字段范围、业务对象和状态枚举。
- 18 个核心路由的本地实机截图，位于 `.codex-screenshots-work-20260817/`。
- 现有产品、架构、部署和可信执行文档，位于 `README.md` 与 `docs/`。
- 未提供独立图像 Logo 文件、生产合规认证材料或现场性能基线；前端不得据此创造声明。

## Product Principles

1. 业务事实优先：界面只解释和组织真实接口信息。
2. 权限边界可验证：入口、路由、操作和数据范围分别受控。
3. 状态闭环清楚：加载、空、错误、超时、提交和处置都有明确结果。
4. 审计信息可追踪：编号、时间、主体、证据、Trace ID 与结果保持一致。
5. 生产效率优先：稳定布局、高信息密度、键盘可用和可预测交互优先于装饰。

## Accessibility & Inclusion

- 支持完整键盘操作、清晰焦点、语义化表单与表格、可访问图标名称和弹层焦点管理。
- 状态不只依赖颜色；正文、控件和状态文本满足 WCAG AA 对比度要求。
- 长机构名、编号、哈希、大金额和 125% 缩放下仍可完成核心操作。
