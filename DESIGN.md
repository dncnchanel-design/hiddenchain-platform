---
name: 隐链明算
description: 多能源可信数据空间的高密度政企业务界面
colors:
  system-bar: "#0A2540"
  primary: "#1768A0"
  primary-hover: "#125681"
  primary-soft: "#EAF5FC"
  accent: "#00A3E0"
  canvas: "#F6F9FC"
  surface: "#FFFFFF"
  ink: "#10233F"
  text-secondary: "#52657C"
  text-muted: "#718196"
  line: "#C9D6E5"
  line-soft: "#E3EAF2"
  success: "#0A8F68"
  warning: "#C87900"
  danger: "#D33A3A"
typography:
  family: '"Microsoft YaHei", "PingFang SC", "Noto Sans CJK SC", Arial, sans-serif'
  page-title: "22px / 700 / 1.3"
  section-title: "15px / 650 / 1.35"
  body: "14px / 400 / 1.55"
  control: "13px / 600 / 1.2"
rounded:
  control: "8px"
  card: "14px"
  overlay: "14px"
---

# 设计系统

## 设计方向

创意主线是“可信数据空间控制台”：参照能源数据空间的企业门户语言，以深海军蓝信息头建立政企可信感，冷灰蓝画布承载高密度业务，白色卡片与细边框区分信息层级，青蓝用于导航与主操作，青绿色只表示真实成功状态。

除系统头和登录背景允许使用克制的深蓝层次外，页面不使用玻璃拟态、营销大标题、装饰性插画或大面积发光效果。可信度来自清晰权限、中文名称、状态来源、授权范围、签名和审计事实。

## 应用壳层

- 78px 深蓝信息头：产品、当前企业、角色、环境和退出。
- 白色一级导航：固定七个业务模块，桌面单行，窄屏转为临时导航；参与主体从右上角“主体中心”进入。
- 冷灰上下文条：显示“可信数据空间 / 业务分组 / 当前页面”。
- 内容宽度按工作台自适应，常规页面边距 20–28px；信息密度目标为 7/10。

平台运维不进入业务壳层，只使用技术运行监控和脱敏日志页面。

## 中文与编号

- 页面标题、导航、按钮、说明、状态和空状态均使用简体中文。
- DID、CSV、API、PSI、MPC、TEE 保持行业缩写，不展开为逐句英文。
- 资源名称为空时统一显示“未命名数据资源，请由提供企业补充中文名称”。
- 英文资源 ID、适配器代码和内部枚举只允许出现在必要的技术调试区域，普通业务卡片和列表不得用其代替名称。
- 任务编号、授权编号、哈希和签名摘要使用等宽字体；它们是审计定位字段，不用作页面标题。

## 组件规则

### 卡片与数据表

- 卡片使用白底、1px 蓝灰边框、14px 圆角和轻微环境阴影。
- 总览指标使用独立状态卡和顶部青蓝识别条；表格承载重复字段，移动端允许表格容器横向滚动。
- 不使用卡片套卡片；详情页以事实条、定义列表、时间线和操作区组织。

### 表单与操作

- 输入、选择器和按钮最小高度 40px，焦点环清晰。
- 每个区域最多一个主按钮；审批、撤销、下载等高影响动作显示前置条件。
- 禁用状态必须说明原因，不能只降低透明度。
- 固定函数选择器不得提供任意代码编辑器或自由公式执行入口。

### 状态

- 成功、处理中、提醒、危险分别使用青绿、蓝、橙、红，并同时显示文字或图标。
- “本地真实能力”“适配器”“未配置”“已阻断”严格区分。
- 本地追加写入记录称为“审计哈希链”或“存证记录”；只有取得真实外部链回执时才显示“区块链锚定”。

## 核心页面

- 登录：居中单卡片，支持演示身份选择、账号密码和 DID 入口；DID 未配置时明确提示，不模拟成功。
- 运行总览：显示当前企业可见目录、授权、任务与待办，不展示虚构健康分。
- 智能数据查询：中文问题解析为能源种类、数据资源、固定函数、日期和授权；最终执行仍由确定性函数完成。
- 数据目录：只显示中文资源名称、提供企业、能源范围、敏感级别和授权状态，不显示英文资源 ID。
- 数据连接：解释企业侧连接器、数据源、函数白名单、数字签名和“原始数据不进入平台”的边界。
- 数据授权：区分“待我审批”和“我的申请”，由提供企业批准或拒绝。
- 隐私计算：展示任务、授权、企业连接器回执、受控结果和签名状态，不展示原始记录。
- 审计追溯：以任务为主线展示授权、计算、签名、哈希链和结果交付记录。
- 参与主体：展示企业、个人账号归属、细粒度权限、DID 元数据和真实能力状态。

## 响应式与无障碍

- 1440×900 为桌面基准；1366×768 保持七项导航可用，右上角“主体中心”入口始终可达。
- ≤700px 使用临时移动导航；中窄屏优先收起导航文字，卡片与详情区按内容逐步改单列；390px 不产生整页横向溢出。
- 支持键盘焦点、语义标签、状态双重编码和减少动画偏好。
- 中文机构长名称可换行或截断并提供完整提示；哈希和编号不撑破容器。

## 禁止模式

- 不用中英重复句、英文资源 ID 回退、模糊“数据问数”等自造词。
- 不把平台运维包装成业务管理员，不给其目录、授权、计算和结果权限。
- 不暗示平台集中存储企业原始数据。
- 不把智能助手生成内容当作正式计算结果。
- 不使用紫色科技风、玻璃效果、巨型圆角、欢迎横幅或无来源统计数字；仅系统头与登录背景允许前述克制深蓝层次。
